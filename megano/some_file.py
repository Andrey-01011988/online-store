# test_black_ignore.py
def example(param1, param2):  # Лишние пробелы - Black исправит
    a = "строка"  # Пробелы вокруг = - Black исправит
    b = 'строка с одинарными кавычками'  # Кавычки останутся (из-за skip-string-normalization)
    c = {"key": 'value'}  # Смешанные кавычки - останутся
    d = "очень длинная строка, которая явно превышает 100 символов, но не будет перенесена из-за skip-string-normalization и потому что это простое присваивание"  # Не перенесётся!
    e = [
        "короткая строка",
        "очень длинная строка в списке, которая тоже не будет перенесена, хотя находится внутри списка, потому что skip-string-normalization=true",
    ]  # Строки в списке не переносятся

    if True:  # Лишние пробелы - Black исправит
        pass
    data = [
        "короткая строка",
        "очень длинная строка, которая не помещается в 100 символов и должна быть перенесена на новую строку из-за того, что находится внутри списка",
    ]

    return {
        "key1": "value1",
        "key2": "value2",
        "key_with_long_value": "очень длинное значение, которое не будет перенесено из-за skip-string-normalization",
    }


result = (
    some_object4.method1()
    .method2()
    .method3("очень длинный аргумент, который не влезает в 100 символов")
    .method("Aaaaaaaaaaaaaaaaaaaaa")
)
message = f"Очень длинное сообщение: {user.name}, ваш заказ {order.id} готов. Пожалуйста, заберите его до {delivery_date}!"
